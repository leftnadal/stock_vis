# SWAP-V0-0820 — SWAP-REVIEW v0 구현 보고서

> 지시서: DIRECTIVE-MON-SWAP-V0-0820 (2026-08-20 계획 세션)
> 브랜치: `monorepo/sess-swap-v0` · **절단 베이스 = `255fa1b9`**(origin/main 최신, D-BRANCH-CUT-FROM-LATEST)
> 산출물: 구현 커밋 + 본 보고서 · **배포는 게이트(§게이트) — 미실행**
> 보고 규율: 명령·출력 verbatim (D-P4-GATE-PROVENANCE)

---

## PART 0 — 정오·등재·위생 (커밋 `44356222`)
- **0-1 ADR v2.1 정오**: §3.11 심도 페어링(EODSignal 520종 ~6개월 / DailyPrice 747종 ~3년 소급 재계산 전제) · §7 "상태 축 4원화(검토 상태 신설)" 명확화 · 개정이력 v2.1.
- **0-2 D-PRIORITY-SWAP-V0**: 채점 프로필 A·⑤+① 0.845 vs ④ 0.585·마진 0.26 verbatim 등재.
- **0-3 D-FIXTURE-FIXED-BASE**: 픽스처 시간 앵커 고정 base 기본 교훈.
- **0-4 동종 time-bomb 정합**: test_v11 now앵커→`_READINGS_BASE`(행위보존·advisor 12/12 green). test_timing_pivot은 `as_of=date.today()` moving이라 time-bomb 없음(무접촉).

## PART 1 — Claim 근거 신호 구조 필드 (BE additive)
- **`ClaimEvidence`**(`apps/monitor/models/evidence.py`): Claim 1:N(related_name="evidences"), kind auto/manual. 자동형={indicator FK(SET_NULL)·operator(gte/lte/gt/lt)·threshold·grace_days=5}. 수동형={description·recheck_period_days=90·last_confirmed_at}.
- **마이그 0010**: CreateModel 단독·`monitor_claim` ALTER 0(additive).
- **판정 서비스** `judge_claim_evidences(claim, as_of)→[{evidence_id,kind,status(alive/dead/expired/unknown),dead_streak_days}]`: 자동형=`score_indicator_dispatch` as_of 재채점+유예 연속성 이력 재계산(신규 상태 저장 0), 수동형=재확인 기한 경과. 순수 함수·DB 쓰기 0·LLM 0.
- **serializer/ViewSet**: `/api/v1/monitor/claim-evidences/`(CRUD·?claim=)·`ClaimSerializer.evidences` nested read.

## PART 2 — 브리핑 근거 생사 점검 라인 (advisor 확장)
- `build_context`에 `evidence` 추가(active Claim의 evidences 판정), `_render_user_prompt`에 "근거 점검" 섹션(additive).
- **무변화 압축**: m==n → "근거 n/n 전부 생존" 1줄. **0/n**: 소멸 경고 선두 지시 + 소멸 지표명+연속일수. **미구조화**: "근거 미등록 — 빌더에서 등록 가능"(강요 톤 0).
- 수치·지표명 = 판정 산출값 **인용만**. **PROMPT_VERSION="v1.1" 무변 · 수치 대조 게이트 미신설**(v0.5 금지 준수, `_lexical_guard`만).
- 스모크(테스트·격리DB·LLM mock): `advisor_briefing_task` 경로 AdvisorNote 생성, note body에 근거 점검 라인 + 기존 본문 형식 무변 verbatim 확인.

## PART 3 — 근거 입력 UI + 2종 대결 화면
### 3-BE (additive, 마이그 0011)
- **`SwapHoldLog`**(hold_logs): candidate_ref·note·**hold_price·candidate_price**(nullable, P-1 성과 앵커). **`DecisionJournalEntry`**(journal_entries): kind(close/recommit/hold)·sentence(**검증 없음**·ADR §6).
- 마이그 0011: CreateModel 2·기존 ALTER 0(additive).
- 엔드포인트: `GET claims/{id}/evidence-status/`(계약층)·`/monitor/swap-hold-logs/`(P-1)·`/monitor/decision-journal-entries/`(일지). P-2b=기존 close+create+journal 조립.
### 3-FE
- **근거 입력**(EvidenceForm/List/Modal, `/monitor/[id]` 진입점): 자동/수동·출처 3색 배지·수동 D-n·고지 2건(관측불가 근거 경고 + 갭 체질 배지 `constants/gapProfile.ts` 출처 주석 RECON E-1).
- **2종 대결**(`/monitor/[id]/swap` 정적 라우트·신규 [id] 0): 층별 카드(계약=evidence-status·마찰=Wallet 어림 "어림" 배지·통계="미장착 v1")·반론 슬롯(규칙 템플릿)·판단 불가·결정 일지 게이트(1문장 전 실행 비활성·품질검증 0)·P-1 보류 계기판(스냅샷 성과·격차, null 앵커 정직표기)·**P-2a**(반론→수동형 근거 등록 CTA)·P-2b(목표 상향 재커밋 배너)·레인 안내 고정 1줄.
- **불변 요소 6종**: 종합 도장 없음·반론 슬롯·결정 일지 게이트·출처 3색 배지·판단 불가·레인 안내 — 전부 구현(파일:라인 FE 보고 참조).

## 자기 검증 (검수 기준 ⑴~⑸)

**⑴ 마이그레이션 additive + 기존 데이터 무손상**:
```
mig 0010: CreateModel(ClaimEvidence) 단독·monitor_claim ALTER 0
mig 0011: CreateModel(SwapHoldLog, DecisionJournalEntry) 2·AddField/AlterField(기존) 0
prod 신규테이블 존재: (None, None, None) = 미적용(배포 게이트 전, 정상)
Claim.count()=6 · Monitor.count()=6 (무손상)
```

**⑵ 브리핑 스모크 문면**: `test_advisor_beat.py::TestAdvisorBeatEvidenceSmoke` — note body에 "근거 점검: 근거 0/2 생존 / ⚠ 등록 근거 전부 소멸… / [자동] momentum_12_1: 연속 6거래일 위반(소멸) / [수동] …재확인 D-90" + 기존 라인(종합 점수/상태/커버리지) 무변 verbatim 확인.

**⑶ 대결 화면 렌더 + 게이트/판단불가/보류 계기판**: FE vitest — DecisionJournalGate 비활성(sentence 공란)·checkJudgmentAvailability 판단불가·HoldGauge 성과·격차 산출·EvidenceForm 렌더 케이스 green.

**⑷ 회귀 기준선**(bb3a69d2 대비 261/0/923 → 신):
```
pytest(monitor): 322 passed (261+20 PART1+12 PART2+29 PART3BE, 회귀 0)
tsc: 0 errors (exit 0)
vitest: 972 passed / 131 files (923+39 PART3FE본체+10 갭, 회귀 0)
```

**⑸ v1.1 문면 계약 무변**: `PROMPT_VERSION="v1.1"` 무변 · 출력⊆프롬프트 수치 게이트 grep 0 · `_lexical_guard`만 유지(v0.5 미착수).

## 게이트 상태 — **배포 미실행·"배포 승인" 대기**
지시서 §게이트 원문: 커밋·push·main ff까지 자율, **배포(BE 워커 재시작·FE 빌드 반영)는 전 파트 검증 보고 제출 + 사용자 "배포 승인" 명시 회신 후에만** 관례 절차(migration-before-restart 포함). → 본 보고 제출 시점 **배포 대기**. prod migrate(0010·0011)·워커 재시작·FE 빌드 전부 **미실행**.

## 미이행·조정 (정직 보고)
- **P-2a 반론→근거**: 규칙 템플릿 반론이 특정 지표 z로 환원 곤란 → **수동형 근거로 등록**(억지 자동형 금지, 사용자가 폼에서 자동형 전환 가능).
- **P-1 후보 성과**: 후보가 Wallet 미보유(순수 감시)면 시세 소스 부재 → `candidate_price` 생략·"현재가 데이터 없음" 표기(보유측만 진실 산출). 감시 종목 시세 API는 v0 스코프 밖.
- **보류 계기판 구 기록**: 스냅샷 신설 이전 로그는 앵커 null → "성과 앵커 없음(구 기록)"·격차 "산출 불가"(조작 0).
- v0.5+(검토 상태기·히스테리시스·수치 게이트·추천 엔진·통계층·기저율) **미착수**(금지 준수).

## 파일 (36 변경: 23 신규·13 수정)
BE: `apps/monitor/models/{evidence,swap}.py`·`migrations/{0010,0011}`·`services/evidence_judge.py`·`services/advisor_briefing.py`·`api/{serializers,views,urls}.py`·`admin.py`·`models/__init__.py` + `tests/monitor/test_{evidence_model,evidence_judge,swap_models,swap_api,advisor_briefing,advisor_beat}.py`
FE: `frontend/` — `constants/gapProfile.ts`·`lib/monitor/{evidence,duel}.ts`·`components/monitor/{SourceBadge,evidence/*,duel/*}`·`app/monitor/[id]/swap/page.tsx`·`app/monitor/[id]/page.tsx`·`types/monitor.ts`·`services/monitorService.ts`·`hooks/useMonitor.ts` + `__tests__/monitor/*`
Docs: `docs/features/swap-review/ADR-D-SWAP-REVIEW.md`(v2.1)·`DECISIONS.md`·`claude/SWAP-V0-0820.md`

---
_전 BE additive·prod migrate 미실행·기존 데이터 무손상. 배포 게이트 정지._
