# ADR — D-MONITOR-HORIZON-ADVISOR

> 상태: **확정 — 사용자 승인 2026-08-04, DECISIONS 등재** (등재 커밋 = 본 커밋)
> 발행: 2026-08-04 계획 세션
> 상위: D-MONITOR-TIMING-PIVOT(질문 교체·리포지셔닝) — 본 ADR은 그 위의 **3층 증축**이며
> 피벗의 불변식(§3.1 EOD·§3.2 가치 축·§3.3 경계·§3.4 행위보존)을 전부 계승한다.
> 관련: D-MONITOR-REBUILD · D-EOD-FRESH(+FIX-1) · MON-DETAIL 방향 결정(계기판+일지 합의)
> · 기지 이슈 chainsight Neo4j · TASKQUEUE MON-CLOSE-UX

---

## 1. 결정 선언

monitor 위에 다음 3층을 **additive**로 증축한다.

1. **HORIZON** — 모든 Monitor에 투자 호흡을 **사용자가 수동 선언**한다:
   `swing`(수일~수주) / `position`(수개월) / `core`(수분기~). horizon은 지표 가중
   프리셋·채점 창·존/기한 기본값·비서의 해석 문맥을 일괄 결정한다.
2. **ADVISOR** — LLM 투자 비서를 **단일 코어 + 3 트리거 표면**으로 설계한다:
   L-A 해설자(스케줄 브리핑) → L-B 상담역(온디맨드 질의) → L-C 파수꾼(이벤트 푸시).
   **설계는 셋을 첫날 완결, 점등은 승격 게이트로 순차.** 비서의 모든 권고는
   AdvisorNote로 기록되고 마감·시장 양방향으로 채점된다 — "적극적 권유"가 허용되는
   근거는 정확성이 아니라 **전수 기록·채점**이다.
3. **T계열(테마 컨텍스트)** — Chain Sight의 테마 그래프를 신호원으로 승격한다.
   테마 모멘텀·폭·상대 강도·앵커 상태를 EOD 스칼라로 산출(기존 S계열 파이프라인
   직결, 신규 기계 없음). 통로는 **스키마 선행 + 수동 시드**(T-B): shared 스키마를
   chainsight가 나중에 그대로 채울 형태로 지금 확정하되, 초기 데이터는 수동 등록.
   chainsight Neo4j 실측·자동 발행은 후속 트랙(CHAINSIGHT-BRIDGE)으로 격리.

## 2. 동기

1. **시간축 혼재**: 스윙 후보와 코어 보유가 같은 지표 가중·같은 잣대로 표시되어,
   IREN −37%(스윙 문맥)와 GOOGL +41%(코어 문맥)가 동일 화면 문법으로 읽힌다.
2. **해석의 부재**: 시스템은 매일 계산하지만 화면에는 결과값만 남고 "왜, 무엇이
   달라졌나"의 서사가 없다(사용자 실불만: "thesis의 변화가 눈에 안 들어온다").
3. **고립 종목 관측의 한계**: 개별 가격·거래량만으로는 "테마 순풍 속 동행"과
   "고유 악재"를 구분할 수 없다. Chain Sight가 이미 보유한 관계 그래프가 미소비 상태.
4. **도그푸딩 목적**: "LLM이 매매 판단 보조에 유용한가"를 본인 데이터로 검증하는 것
   자체가 이 증축의 1차 목표다. 유용하지 않다는 결론도 수확이다(게이트 불통과 = 답).

## 3. 확정 불변식 (이 ADR과 함께 잠금)

- **I-1 단방향**: LLM 출력은 어떤 경로로도 indicator_scorer·aggregator·
  state_machine·zone 계산에 되먹이지 않는다. 비서는 엔진의 **소비자**다.
  (위반 시 캘리브레이션이 순환 오염 — 권고가 점수를 만들고 점수가 권고를 정당화.)
- **I-2 전수 기록**: AdvisorNote 없는 권고는 존재할 수 없다. 표면(브리핑·답변·알림)은
  Note의 렌더링일 뿐이다. 프롬프트는 버전 태그를 갖고, 채점은 프롬프트 버전 단위로
  집계 가능해야 한다.
- **I-3 격리 배치**: Advisor는 monitor 도메인 내부 서비스(`services.advisor` 상당)로
  두어 앱 간 신규 결합을 만들지 않는다(leaf 유지). 전 표면은 기능 플래그로 on/off.
  서비스화 재조립 시 이 모듈 단위로 제거·강등("권유→정보 제공") 가능해야 한다.
- **I-4 주체 분리 계승(§3.2)**: 비서는 권유까지 한다(적극 허용). 판정·행동은 사용자.
  **자동 주문 실행은 영구 범위 밖**(Wallet read-only 불변). 모든 권고 표면에 주체
  문구 상주.
- **I-5 EOD 계승(§3.1)**: `swing`은 스윙(수일~수주)이다 — 장중은 영구 범위 밖.
  horizon 도입이 실시간화의 근거가 되지 않는다.
- **I-6 additive-only**: 전 스키마 변경은 추가형. 기존 엔진·beat·마감 루프 구조 불변.
  T계열은 스칼라 지표로만 편입(E계열 격리 원칙 유지 — 패턴 인식기 신설 없음).
- **I-7 규제 예약**: 개인 도그푸딩 단계는 권유 강도 제약 없음. **서비스화 착수 시점에
  투자자문 규제 전문 검토를 선행**한다(이 ADR은 법률 판단이 아님). I-3이 그 대비책.

## 4. HORIZON 설계

### 4.1 분류와 선언 (H-A 채택 — 수동, 기각: H-B 자동 추론)

- `Monitor.horizon ∈ {swing, position, core}` — 생성 시 필수 선택, 편집 가능.
  변경 이력은 보존(캘리브레이션 시 "호흡 변경 후 성적" 분리 집계 대비).
- 철학: 이 시스템은 의도를 **커밋받는** 시스템이지 짐작하는 시스템이 아니다.

### 4.2 지표 가중 프리셋 (weights 합 = 1.00 · 아래 표가 초기 확정값)

점수 기여 지표 = 기존 S계열 6종 + T계열 2종(테마 모멘텀·상대 강도).
T계열의 폭(breadth)·앵커 상태는 **비점수 컨텍스트**(§6.3 — 테마 수준 값이라 동일
테마 내 종목 간 차별력이 없어 집계 왜곡 방지, 표시·비서 인용 전용).

| 지표 | swing | position | core |
|---|---|---|---|
| 장기 추세(200일 괴리) | 0.05 | 0.15 | 0.30 |
| 모멘텀 12-1 | 0.05 | 0.25 | 0.15 |
| 52주 고가 근접 | 0.15 | 0.15 | 0.05 |
| 거래량비 | 0.20 | 0.07 | 0.05 |
| MACD 히스토그램 | 0.15 | 0.08 | — (0) |
| 과매도 오실레이터 | 0.20 | 0.05 | 0.05 |
| **T: 테마 모멘텀** | 0.10 | 0.15 | 0.15 |
| **T: 상대 강도(종목/테마)** | 0.10 | 0.10 | 0.05 |
| **가치평가 통로(적정가 괴리)** | — (0) | — (0) | 0.20 |
| **합** | **1.00** | **1.00** | **1.00** |

- **재정규화 규칙**: null 지표(테마 미등록 시 T계열, 통로 미개통 시 가치평가)는
  해당 가중을 잔여 지표에 비례 배분한다: `w'_i = w_i / (1 − Σ w_null)`.
  (예: core에서 가치평가 null → 전 가중 ÷ 0.80.)
- 근거 강도 메타(강/중/약)는 기존 원칙대로 UI 병기. T계열 근거는 §6.1.
- 프리셋은 초기값이며, 마감 ≥20건 후 지표별 승률 집계(기존 백로그)에서
  **horizon별로 분리 재보정**한다 — 이 표 자체가 캘리브레이션 대상.

### 4.3 horizon이 결정하는 그 외 기본값

| | swing | position | core |
|---|---|---|---|
| 기한 기본값 | 20거래일 | 120거래일 | 500거래일 |
| 손절 폭 제안(L계열 ATR 배수) | ×1.5 | ×2.5 | ×3.5 |
| 채점 창 W(§5.6) | 5거래일 | 20거래일 | 60거래일 |
| 채점 무승부 대역 τ | ±1.0% | ±2.0% | ±3.0% |

기본값은 빌더의 **제안 초기치**일 뿐 — 사용자 수정 자유(사전 커밋 주체 불변).

## 5. ADVISOR 설계 (단일 코어)

### 5.1 코어 구성

```
ContextBuilder → PromptTemplate(versioned) → LLM 호출 → 출력 검증 → AdvisorNote 기록
                                                              ↓
                                             표면 렌더링 (브리핑 / 답변 / 알림)
                                                              ↓
                                        채점기 (시장 채점 + 마감 채점) ← 마감 도장
```

- **ContextBuilder**: MON-DETAIL 상세 페이지와 **동일 조립기 공유**(단일 소스).
  입력: 존·사다리, 지표 8종 값+강도, horizon, 논거 축 상태, 일지 최근 10거래일,
  테마 패널(등록 시), 기한 잔여, 손익, 직전 AdvisorNote 요약 1건.
  직렬화 후 `context_hash`(sha256) 산출 — 재현성 앵커.
- **LLM 호출**: Anthropic API. 모델·버전은 config(하드코딩 금지), 호출은 기존
  api_request 계열 circuit_breaker 경유. `model_id`를 Note에 기록.
- **출력 검증**: JSON 스키마 강제. 불통과 시 1회 재시도 → 실패면 해당 표면 결측
  처리(엔진 무영향 — I-1의 실패 모드 배당).

### 5.2 AdvisorNote 스키마 (신설 · additive)

```
AdvisorNote {
  id, monitor_fk, claim_fk(nullable),
  trigger_type: scheduled | on_demand | event,     # 첫날부터 3값 예약
  stance: SELL_REVIEW | TRIM_REVIEW | HOLD | ADD_REVIEW | ENTER_REVIEW,
  confidence: low | mid | high,
  rationale: [≤3, 각 항목은 cited_signals 참조 필수],
  counterarguments: [≥2, 최소 1개는 stance 반대 방향 필수],
  cited_signals: [지표/테마 신호 키 목록],          # 서사를 숫자에 결박
  falsification_check: 논거(반증 조건) 대조 결과 요약,
  prompt_version, model_id, context_hash,
  created_at,
  market_score: hit | miss | push | pending | na,   # §5.6
  closure_score: aligned | opposed | na             # §5.7
}
```

### 5.3 스탠스 5단 (확정)

`SELL_REVIEW`(매도 검토) · `TRIM_REVIEW`(부분 매도 검토) · `HOLD`(보유) ·
`ADD_REVIEW`(추가매수 검토) · `ENTER_REVIEW`(신규진입 검토 — new_entry 시나리오 전용).
- 부분 매도를 정식 스탠스로 승격하는 이유: 목업 검토에서 자연 발생했고,
  "양쪽 오류 비용 절충"이라는 고유한 권고 의미를 가지며, 채점 방향이 SELL과
  동일(하방 유리)이라 채점 규칙 복잡도를 늘리지 않는다.
- 채점 방향 매핑: {SELL, TRIM} → 하락 적중 / {ADD, ENTER} → 상승 적중 /
  HOLD → **방향 채점 제외**(§5.6의 na). 대신 §5.8 HOLD 과다 경보로 관리.

### 5.4 확신도 3단 (확정 — 퍼센트 기각)

low/mid/high. 캘리브레이션 요건: 표본 누적 시 **적중률 단조성**(high ≥ mid ≥ low)을
분기별 점검 — 위반은 게이트 차단 사유가 아니라 **프롬프트 개정 트리거**(비서의
과신/과소신 교정 신호).

### 5.5 프롬프트 불변 요구 (전 버전 공통 계약)

⑴ 근거 ≤3 · 반론 ≥2(1개는 반대 방향) · 스탠스·확신도 필수 ⑵ 근거·반론은
cited_signals의 정량값 인용 없이는 무효 ⑶ 사전 커밋(존·기한·논거) 대조를 의무 포함
⑷ horizon 문맥 명시("포지션 호흡 기준 …") ⑸ 주체 문구("판단의 주인은 사용자") 상주
⑹ 테마 서사는 테마 패널 신호 인용 시에만 허용 + 상관의 거울상(동반 하락 위험) 인지.

### 5.6 시장 채점 (기계 채점 · 자동)

권고 시점 종가 C₀, horizon별 창 W 거래일 후 종가 C_W, 수익률 r = C_W/C₀ − 1.
- {ADD, ENTER}: r > +τ → **hit** / r < −τ → **miss** / |r| ≤ τ → **push**(제외)
- {SELL, TRIM}: r < −τ → **hit** / r > +τ → **miss** / |r| ≤ τ → **push**
- HOLD → na. τ·W는 §4.3 표. 창 도달 전 마감 발생 시 마감일 종가로 조기 채점
  (플래그 병기).

### 5.7 마감 채점 (사용자 판정과의 정합)

마감 도장(익절/손절/기한만료) 시점에, 마감 직전 P거래일(=창 W) 내 Note들을 소급:
권고 방향과 마감 결과의 정합(aligned/opposed) 기록. 시장 채점과 별개 축 —
"비서가 시장을 맞혔나"(5.6)와 "비서가 내 실제 결정과 결과에 유용했나"(5.7)는
다른 질문이다.

### 5.8 운영 가드

- **HOLD 과다 경보**: 직전 10거래일 Note 중 HOLD 비율 > 80% 지속 시 프롬프트 개정
  트리거(판단 회피 = 채점 회피 봉쇄).
- **브리핑 생성 범위(확정)**: 전 종목 매일 생성. 단 "특이사항 없음" 판정 시 2문장
  압축 모드(비용·소음 절감, 스탠스·확신도는 항상 기록). 특이사항 정의 초기 규칙:
  거래량 ≥1.5× ∨ 지표 방향 전환 ∨ 존 이동 ∨ 논거 상태 변경 ∨ 테마 이탈 감지.
- **비용 상한**: 일 호출 수 상한 config(초기 6종 × 1 + 온디맨드 여유 = 20).
  circuit open 시 표면 결측 + 로그(무성 실패 금지 — beat 교훈).
- **시간창**: advisor 배치는 refresh_monitors **성공 후 체이닝**(독립 스케줄 금지 —
  stale 컨텍스트로 브리핑 생성 방지 + beat 시간창(#28 DB 기준) 경합 명시 관리).

### 5.9 점등 단계와 승격 게이트 (사전 커밋)

| 단계 | 지금 짓는 것 | 점등 조건 |
|---|---|---|
| L-A 해설자 | 코어 전체 + scheduled 트리거 + 브리핑/카드 칩 표면 | ADVISOR-P1 배포 즉시 |
| L-B 상담역 | 플래그 자리만 | **게이트 1**: AdvisorNote ≥60 ∧ MON-DETAIL P1 완료 ∧ 주간 정성 체크("브리핑이 읽을 가치 있었나") 4주 중 ≥3주 긍정 |
| L-C 파수꾼 | 플래그 자리 + trigger_type=event 예약 | **게이트 2**: 실마감 ≥10 ∧ 방향 채점 표본(SELL·TRIM·ADD·ENTER, push 제외) n ≥40에서 적중률의 Wilson 90% 신뢰 하한 ≥ 0.35 ∧ 확신도 단조성 위반 없음(또는 개정 완료) ∧ 반론 품질 정성 통과 ∧ L-A 로그에서 유용 이벤트 패턴 ≥3종 실증 |

- 게이트 2 통계 기준의 의도: "우수 증명"이 아니라 **"무작위(0.5) 대비 명백한 열위
  부재"의 방어선**이다(n=40에서 관측 적중률 대략 ≥47% 요구). 표본 미달 시 기간이
  아니라 표본으로 기다린다.
- L-C 점등 시 함께 확정할 것(그 시점 결정 사이클): 이벤트 트리거 규칙(존 전이·
  손절선 x% 접근·거래량 급증·논거 훼손·테마 이탈), 쿨다운(동일 종목·동일 사유
  3거래일), 일일 알림 상한, "오늘은 그만" 제어.
- **게이트 불통과의 지위**: 실패가 아니라 도그푸딩의 답("이 용도에 LLM은 아직
  유용하지 않다")으로 기록된다.

## 6. T계열 설계 (테마 컨텍스트)

### 6.1 신호 4종 (전부 EOD 스칼라 — 기존 파이프라인 직결)

| 신호 | 정의(초기) | 소비처 | 근거 강도 |
|---|---|---|---|
| 테마 모멘텀 | 멤버 12-1 모멘텀의 근접도 가중 평균 (z) | **점수 기여** | 중~강 (Moskowitz & Grinblatt 1999 산업 모멘텀) |
| 상대 강도 | 종목 20일 수익률 ÷ 테마 20일 수익률 | **점수 기여** | 중 |
| 테마 폭 | 멤버 중 20일선 上 비율 | 컨텍스트(비점수) | 중 |
| 앵커 상태 | 앵커 종목의 추세(200일)·모멘텀 요약 | 컨텍스트(비점수) | 중 (Cohen & Frazzini 2008 경제 링크 전이 · Lo & MacKinlay 1990 선행 효과) |

- 비점수 사유: 폭·앵커는 테마 수준 값 — 동일 테마 종목 전부에 같은 값이 들어가
  차별 정보가 없고, 비서 인용·화면 표시로 충분.
- **이탈(decoupling) 감지**: 상대 강도의 급락(초기 규칙: 20일 상관 < 0.4 ∧ 상대
  강도 < 0.85)은 "테마는 좋은데 종목만 처짐" = 고유 문제 경고로 표시·브리핑 인용 —
  논거 축 점검 유도. 순풍 서사의 거울상을 시스템이 먼저 말하게 한다.
- 전 신호는 지표별 승률 집계 대상(마감 ≥20건 후) — 테마 신호도 채점받는다.

### 6.2 통로 스키마 (shared 신설 · T-B: 스키마 선행 + 수동 시드)

```
ThemeGroup      { id, name, source: chain_sight | manual, active, updated_at }
ThemeMembership { theme_fk, symbol, proximity: 0..1, is_anchor: bool, active, updated_at }
```

- 제약: 테마당 멤버 ≤ 8(비용 상한) · 종목당 테마 ≤ 3 · 앵커는 테마당 ≤ 2.
- 초기 운용: 사용자 수동 시드(보유 6종 기준 테마 2~3개, 10분 작업).
- **CHAINSIGHT-BRIDGE(후속 별도 트랙)**: Neo4j 상태 실측 → chain_sight가 이 스키마로
  주기 발행(source=chain_sight) + 수동 행과의 병합 규칙. 이 ADR은 스키마만 확정하고
  브리지 착수는 별도 결정 — 본 증축이 타 앱 건강에 인질 잡히지 않게 하는 격리다.
- 경계 준수: 앱 간 직접 import 없음(shared 경유), monitor leaf 불변.

### 6.3 데이터 비용 (게이트 유니버스 확장)

- 신선도 게이트 검사 대상을 `보유 심볼 ∪ 활성 테마 멤버`로 확장(FIX-1 검증 경로
  재사용). 상한: 테마 3개 × 8멤버 = 최대 +24심볼 → FMP 일 콜 최대 +24(전부 stale인
  최악일 기준, 편입 멤버는 0콜). RECON에서 실콜 수·게이트 elapsed 시뮬레이션 실측
  후 상한 조정.

## 7. 기각한 대안 (기록)

- **H-B 자동 horizon 추론** — 기각. 신규 추론 기계 + 오분류 시 가중 전체 오염 +
  "의도를 짐작하는 시스템"으로의 철학 역행.
- **L-A/B/C 동시 전면 구축** — 기각. 설계는 일체(채택), 구축 동시는 파수꾼 트리거
  설계가 L-A 운용 데이터 없이는 열화되므로 손해. (착수 순서 가중: L-A 0.825 /
  L-B 0.735 / L-C 0.523.)
- **LLM 출력의 점수 되먹임(비서 의견을 지표화)** — 기각. I-1 위반, 캘리브레이션
  순환 오염.
- **T-A 완전 자동 통로 선행** — 기각(0.639 vs T-B 0.828). chainsight Neo4j 기지
  이슈에 착수가 인질.
- **T-C monitor 자체 테마 소유** — 기각(0.652). 이중 진실 + 발굴↔검증 지도 훼손.
- **확신도 퍼센트 표기** — 기각. 소표본 과신 유발, 3단이 단조성 점검에 충분.
- **장중 신호 재론** — 기각(I-5, §3.1 계승).

## 8. 스테이징 (각 트랙 = 별도 지시서·결정 사이클, 표준 절차 RECON→목업→결정→구현)

| # | 트랙 | 내용 | 선행 조건 |
|---|---|---|---|
| 0 | **HA-P0 RECON** | 읽기 전용 통합 실측: Claim 기한 필드 현황·가중 주입 지점(aggregator 프리셋 수용 구조)·snapshot/reading 이력 보존 범위·상세 API 유무·DailyPrice.volume·NewsArticle 심볼 매핑 실태·알림 인프라 구조·Anthropic API 호출 경로(api_request 재사용성)·FMP 콜 시뮬레이션·chainsight Neo4j 상태(관찰만) | 이 ADR 승인 |
| 1 | MON-DETAIL-P1 | 상세 라우트 + 계기판 헤더 + 일지(가격·거래량·지표Δ) — dangling link 해소 | P0 |
| 2 | HORIZON-P1 | horizon 필드 + 가중 프리셋 + 재정규화 + 배지 | P0 (P1과 병렬 가능성은 RECON 판정) |
| 3 | THESIS-P2 | 논거 축(반증 조건 ≤3 · 토글 판정 · 물타기 경고) | P1 |
| 4 | ADVISOR-P1 | 코어 전체 + L-A 점등(브리핑·카드 칩) + 채점기 | P1·P2 (컨텍스트 완결성) |
| 5 | THEME-P1 | T계열 신호 + 통로 스키마 + 수동 시드 + 게이트 유니버스 확장 → 브리핑 테마 인지형 승격 | P0 (P4와 순서 교환 가능 — RECON 후 확정) |
| 6 | NEWS-P1 | 일지·테마 뉴스 열 — **조건부**(RECON의 심볼 매핑 품질 판정에 종속) | P0 판정 |
| G1 | — | **게이트 1 심사** → ADVISOR-P2 (L-B 상담역) | §5.9 |
| 7 | CHAINSIGHT-BRIDGE | Neo4j 실측 → 자동 발행 결합 (별도 결정 사이클) | 독립 |
| G2 | — | **게이트 2 심사** → ADVISOR-P3 (L-C 파수꾼, 트리거 규칙 결정 사이클 동반) | §5.9 |

## 9. 리스크 등재

| 리스크 | 대응 |
|---|---|
| 비서 아첨·과신 | 반론 ≥2(반대 방향 1 필수)·확신도 단조성 점검·HOLD 과다 경보(§5.8) |
| 파수꾼 늑대 소년화 | 게이트 2 후 점등 + 쿨다운·일일 상한·사용자 제어("오늘은 그만") |
| 상관의 국면 전환(테마 순풍 서사가 하락장에서 역효과) | 프롬프트 계약 ⑹ 거울상 의무 인지 + 이탈 감지 + T계열도 승률 채점 |
| 수동 시드 테마의 부패(멤버십 낡음) | ThemeGroup.updated_at 표시 + BRIDGE 트랙이 근본 해소 |
| LLM API 장애·비용 | circuit_breaker 경유·일 상한·표면 결측 무해 설계(I-1) + 결측 로그(무성 실패 금지) |
| beat 시간창 경합 | advisor는 refresh 성공 후 체이닝(§5.8) — 독립 스케줄 금지 |
| 채점 규칙 자체의 왜곡(τ·W 오설정) | 초기값을 ADR에 잠그되 마감 ≥20건 재보정 대상으로 명시(§4.2와 동일 지위) |
| 서비스화 시 규제 | I-3 모듈 격리 + I-7 전문 검토 선행 예약 |
| HOLD 남발로 채점 회피 | §5.8 경보 → 프롬프트 개정 트리거 |

## 10. 이 ADR이 확정하는 것 / 확정하지 않는 것

- **확정**: 3층 증축과 그 불변식 7개 · horizon 3분류(수동)와 가중 프리셋 초기값·
  재정규화 규칙 · Advisor 단일 코어와 AdvisorNote 스키마·스탠스 5단·확신도 3단·
  프롬프트 계약·채점 수식(시장/마감 2축, τ·W)·점등 단계와 승격 게이트 수치 ·
  T계열 신호 4종과 점수/컨텍스트 구분·통로 스키마(T-B)·게이트 유니버스 확장 원칙 ·
  스테이징 순서 · 기각 대안 기록.
- **미확정(후속)**: 각 필드의 정확한 물리 스키마(HA-P0 실측 후) · P1↔P2, P4↔P5의
  병렬/순서(RECON 판정) · NEWS-P1 착수 여부(매핑 품질 종속) · 파수꾼 이벤트 트리거
  세칙(게이트 2 시점 결정 사이클) · BRIDGE 병합 규칙 · 프리셋·τ·W의 재보정 값
  (마감 ≥20건 후) · 테마 초기 시드 목록(사용자 입력).
