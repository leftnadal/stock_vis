# 지시서 ⑲ 실행 보고 — Neo4j 동결 완결 + ego cross_edges + 중심성(S-C) 착공 + discovery 정체 실측

**세션**: 실행(S4만 read-only) · **날짜**: 2026-07-16
**worktree**: `monorepo/sess-19-centrality` (origin/main `46e6865`에서 분기)
**HALT 미발동** (HALT-1/2/3 해당 없음) · **Neo4j 기동 0 · 외부 API 0**

---

## 1. STEP 0 — 경량 ground truth

| 항목 | 실측 |
|---|---|
| origin/main HEAD | `46e6865` (⑱ 브랜치 `sess-18-survey` **미착지** — 파일 무겹침이라 진행) |
| pytest baseline | **3872 passed · 53 skipped · 0 failed** (240s) |
| RC / RPS | 13,697 / 133,868 |
| celery dict 3블록 | ⑰-M이 이미 주석 처리 상태(라인 718-726·742-744) — ⑲에서 완전 제거 |

**RC 신규(first_observed) 최근 추이** (S4 사전 수치): 06-21(121) → **07-02(2)** → 이후 0. 주간(토요일) 유입이 06-21 이후 끊김.

---

## 2. S1 — celery.py neo4j sync dict 3블록 완전 제거

- ⑰-M이 주석 처리했던 3블록(profiles/relations/dirty-sync)을 `beat_schedule`에서 **완전 제거**(주석 stub 0). 간결 포인터 2줄 + DECISIONS(D-NEO4J-FREEZE)로 원 스케줄 이관.
- **보존**: 태스크 함수·DB PeriodicTask·task_routes queue:neo4j 3배정(라인 56-58).
- **검증**: celery app 로드 OK(80 beat 엔트리), chainsight neo4j beat 엔트리 **0**, 주석 stub **0**. 마이그레이션 무발생(celery.py=모델 아님).
- **DB 실측**: neo4j sync 3태스크 이미 `enabled=False`(⑰-M) — dict 제거로 #28 재동기 부활 경로 차단.

## 3. S2 — ego API cross_edges 흡수 (A1 부분 흡수)

- `GET /chainsight/ego/<symbol>/`에 `include_cross_edges`(기본 false).
- **기본 false = 응답 바이트 불변**(cross_edges 키·meta.cross_edges_count 부재) — additive 증빙(테스트 `test_default_false_is_additive`·`test_explicit_false_same_as_default`).
- true 시: limit 절단 후 이웃 집합 사이 RC 엣지를 top-level `cross_edges`(동일 edge 스키마+trend) + `meta.cross_edges_count`. **단일 양방향 쿼리**(N+1 방어 테스트 통과), 궤적은 기존 RPS 단일 조회에 cross 쌍 합류.
- 테스트 6종 + 기존 8 = **14 passed**.

## 4. S3 — 중심성 일간 배치 착공 (S-C, Neo4j 불사용)

- **networkx 정식 의존 승격**: `pyproject.toml` `networkx = "^3.6"` + `poetry.lock` content-hash 정합(networkx 3.6 이미 lock에 존재 → diff 1줄).
- **모델** `SymbolCentrality`(apps/chain_sight): symbol(CharField)·as_of·pagerank·betweenness·pagerank_rank·betweenness_rank·graph_nodes·graph_edges. (symbol,as_of) unique, 일별 append.
- **태스크** `compute_symbol_centrality`: RC 전량 → networkx 무방향 가중 그래프 → PageRank+betweenness → update_or_create(멱등). 소요>10s 시 warning.
- **조회 API** `GET /chainsight/centrality/top/?metric=pagerank|betweenness&n=&as_of=`.
- **ego rank 필드**(additive): ego 노드에 `pagerank_rank`·betweenness_rank`(최신 as_of, 없으면 null).
- **테스트** 14종(서비스 6·태스크 3·조회 API 4·ego rank 1) 통과.

### ★ 드라이런 대조 일치 (prod read-only, 2026-07-16)

| | ⑱ 드라이런 | ⑲ 서비스(첫 실행) | 일치 |
|---|---|---|---|
| nodes / edges | 555 / 9,551 | **555 / 9,551** | ✅ |
| PageRank top10 | NVDA GOOGL MSFT AAPL HPE META CSCO MSI GOOG ACGL | **동일** | ✅ |
| Betweenness top | NVDA TSLA GOOGL GEV CDNS WMT CTVA CAT CEG LLY FSLR INTC | 동일 모집단(정확값 = 샘플 대비 소폭 재배열) | ✅(개선) |
| 소요 | 3.92s (k-샘플) | **1.95s** (정확) | ✅ |

> 로직 = ⑱ 동일(collapse weight=max(truth,market), PageRank 가중). betweenness는 프로덕션 정확 계산(드라이런 k=300 샘플 제거 — 555노드는 정확값도 <2초).

### beat 스펙 (등록 금지 — 명세만, Bug #28)

| 항목 | 값 |
|---|---|
| 이름 | `chainsight-daily-centrality` |
| 태스크 경로 | `apps.chain_sight.tasks.centrality_tasks.compute_symbol_centrality` |
| 스케줄 | 일 1회, **17:00 UTC(12:00 EST) 이후** = RC 갱신 체인(relation-confidence 11:00 EST + pair-aggregation 11:30 EST) 후속 |
| 등록 | **DB-only PeriodicTask**(dict 금지) — 병진 수동 |

### sqlmigrate (0018_symbolcentrality — 적용은 병진)

```sql
CREATE TABLE "chainsight_symbol_centrality" (
  "id" bigint NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  "symbol" varchar(10) NOT NULL, "as_of" date NOT NULL,
  "pagerank" double precision NOT NULL, "betweenness" double precision NOT NULL,
  "pagerank_rank" integer NOT NULL, "betweenness_rank" integer NOT NULL,
  "graph_nodes" integer NOT NULL, "graph_edges" integer NOT NULL,
  "created_at" timestamp with time zone NOT NULL);
ALTER TABLE ... ADD CONSTRAINT ... UNIQUE ("symbol", "as_of");
CREATE INDEX ... ("as_of","pagerank_rank"); ("as_of","betweenness_rank"); ("symbol","as_of" DESC);
```
**신규 테이블 1개만 — 기존 테이블 ALTER 0**(additive 증빙). S1·S2 마이그레이션 무발생.

## 5. S4 — discovery 배치 정체 실측 (read-only, 조치 0)

**질문: "해자 폭(신규 RC)이 왜 06-21 이후 안 자라는가."**

### RC 신규 행 생성 경로 (전수)

| 경로 | 타입 | 파일 | beat(DB) | last_run | enabled |
|---|---|---|---|---|---|
| extract_co_mentions | CO_MENTIONED | relation_tasks.py | chainsight-co-mentions | **2026-07-15** total=93 | ✅ |
| update_relation_confidence | PEER_OF | relation_tasks.py | chainsight-relation-confidence | **2026-07-15** total=93 | ✅ |
| calculate_price_co_movement | PRICE_CORRELATED | relation_tasks.py | chainsight-price-co-movement | 2026-07-11 total=13 | ✅ |
| seed_relations_to_chainsight | SUPPLIES_TO 등 | sec_pipeline/tasks.py | sec-seed-relations-to-chainsight | **2026-07-15** total=83 | ✅ |
| (입력) collect_av_broad_news | — | news/tasks.py | collect-av-broad-news | **2026-07-16** | ✅ |
| neo4j sync 3종 | — | sync_tasks.py | (3건) | 07-12/13 | **False(동결)** |

### 정체 원인 분류 → **전 경로 (b) "돌지만 신규 0"**

- **DB PeriodicTask 실측**: discovery 태스크 **전부 enabled=True·최근 실행**(co-mentions/relation-confidence 07-15 total=93). → **(a) beat 미등록/비활성 아님.**
- **(c) 실패 아님**: total_run_count 정상 증가.
- **근본 원인 = (b) + (d) 복합**:
  - **입력 단절(d 흔적)**: 2026-04-25 `df85496` "Alpha Vantage per-symbol 전면 제거"(무료 25/day 예산 초과) → co-mention의 유일 신규-쌍 소스인 다종목 뉴스 입력 중단. 재설계 broad는 07-02 `43b7c7f`·beat 등록 07-08 `f22eb8a`.
  - **유니버스 포화(b 본질)**: 고정 S&P500(~500 심볼)에서 4월 대량 생성(04-03·04-12·04-19) 후 대부분 qualifying 쌍이 이미 존재 → update_or_create가 **기존 갱신만**, 신규 ROW는 새 쌍이 임계 통과할 때만. PEER_OF/PRICE 경로는 Neo4j PEER_OF 쌍(동결로 정적) 대상이라 신규 쌍 0.
- **06-21 관찰의 의미**: 주간 배치(05-17~06-21)는 그 주 이전 수집분 재처리. 06-21이 입력 있던 마지막 주 → 이후 신규 0.

### 재가동 비용 산정

| 경로 | 비용 | 근거 |
|---|---|---|
| co-mention 입력 | **소** | broad AV 이미 재개(07-08)·enabled. 단 다종목 비율 14%·S&P500 내 신규 쌍 희소 |
| price/peer(Neo4j 소스) | **중** | Neo4j PEER_OF 정적(sync 동결) → 신규 쌍은 Neo4j seed 재활성 필요 |
| **해자 실질 재성장** | **대** | 고정 유니버스 포화가 근본 → **유니버스 확장(S&P500 밖) 또는 신규 관계 소스**가 있어야 신규 유입. 설계 결정 사안 |

**예상 신규 유입**: 과거 주간 baseline 60~130건/주(05~06월). 입력만 복구 시 4월 co-mention baseline ~17/일 수준 하한. **정밀 규모는 유니버스 정책에 의존 — 조치는 별도 결정(TASKQUEUE Q19-DISCOVERY-REACT).**

**HALT-3 미해당**: "돌지만 실패"가 아니라 "돌지만 신규 0" — 외부 API 장애/인증 이슈 없음.

## 6. S5 — 원장 정산

- **DECISIONS 3건**: D-NEO4J-FREEZE(트리거 정교화 ~10만 엣지/실시간 다중홉 + 원 스케줄 보존) · D-A2-DEEPDIVE(폐기) · D-SC-CENTRALITY(착공).
- **TASKQUEUE 5건**: Q19-PYTEST-FILTERWARN(저우선) · Q19-REDUNDANT-SIGNAL(정규화 연결) · Q19-SD-LINKPRED(예약) · Q19-A3-SECTOR-MOCKUP(⑳ 회부) · Q19-DISCOVERY-REACT(조치 보류).
- **PROGRESS** 갱신.

---

## 회귀 (DoD 게이트)

- 전체 pytest: **3892 passed · 53 skipped · 0 failed** (baseline 3872 +20 신규[centrality 14·cross_edges 6], 신규 red 0)
- S2 기본값 응답 바이트 불변: `test_default_false_is_additive`·`test_explicit_false_same_as_default` 통과
- S3 드라이런 대조: PageRank top10·nodes/edges 완전 일치(위 표)
- S1·S2 마이그레이션 무발생 / S3 신규 테이블 sqlmigrate 첨부(위)
- 프론트 무변경(vitest 무영향): frontend/ 파일 변경 0

---

## 병진 수동 대기 (순서 필수)

1. ⑱ 커밋(미착지) · ⑲ 브랜치 머지 (파일 무겹침 — 함께 착지 가능)
2. **prod migrate** (S3 테이블 `chainsight_symbol_centrality`, 0018)
3. **sv sync → daphne·worker·beat 재시작** (런타임 3트리 반영)
4. **재시작 후** neo4j_sync DB 3건 `enabled=False` 확인(이미 False지만 순서상 재시작 후 재확인 — 구 beat 가동 중 전환 시 dict 동기화 되살림 방지. 단 ⑲가 dict를 제거했으므로 되살림 경로 자체 소멸)
5. **중심성 beat DB-only 등록**: `chainsight-daily-centrality` @ 17:00 UTC 이후, task=`apps.chain_sight.tasks.centrality_tasks.compute_symbol_centrality` (dict 등록 금지)
6. worktree 정리 · ⑰-M STEP 5(ego 실물 5관점) 메모
