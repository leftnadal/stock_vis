# SECB-V2-ROLLOUT 정찰 보고 — 전제 4건 + 서열 판정 (2026-08-10, read-only)

> **세션**: `monorepo/sess-secb-v2-recon` · base `f27bca59` · write 0·LLM 0(기존 로그만)
> **§0 baseline**: clock 2026-08-10 19:47 ET / 08-11 08:47 KST · health 13 OK / 2 WARN(#47·blocked, 무해) / 0 FAIL

---

## §0-보정 — 서열 판정 (V-B 재판정 437, 원장 심판)

| 항목 | 원장 문면 | 판정 |
|---|---|---|
| **437 not_found 분해·재판정** | G1.5(DECISIONS, 07-28): 결정론 분해(LLM 0콜) → 437→유니크 182, tail 발산 **169(92.9%)**·재서술5·합성8 | **✅ 종결** |
| **V-B 부분도입** | `SECB-V-B-STANDBY`(07-31): 트리거=순수 nf율 **>15% 재발 시에만**. G1.6 재분류로 잔여 2.03% ≤15% → **미발동**. 선제 도입 금지(V-B=2콜, V-A 결정론 계약 위배) | **💤 STANDBY·미발동** |

**서열 결론**: V-B/437은 SECB-V2-ROLLOUT의 **선행도 병행도 아닌 하류 조건부**. G1.5 Why 명시 = "prompt v2 롤아웃 후 재측정 우선, V-B는 그 후 잔여 합성/재서술 ~13에만 조건부". → **롤아웃이 먼저**, 롤아웃 후 nf 재측정이 >15%면 그때 V-B. 메모리 상충("종결 vs 다음 주작업") 해소 = 종결 맞음, V-B는 대기 컨틴전시. **롤아웃 착수 블로커 없음.**

## §1 — 전제 4건 실측 (전수, `SupplyChainEvidence`)

| # | 전제 | 실측 | 결정 함의 |
|---|---|---|---|
| **[컬럼]** ⑴ | `evidence_text` 그릇·fill률 | `TextField()`(무제한 `text`·NOT NULL) **실존**. total **1768**·fill **100%**(빈값 0)·prompt_version 전부 v1·grounding_method: deterministic_v1 **1751**+None 17. **마이그 불요** | 그릇 이미 있음. 스키마 변경 0 |
| **[길이]** ⑵ | v1 인용 길이 분포(전수) | p50=**208**·p90=p99=max=**300**·>300=**0%**. **정확히 300 도달 409행(23.1%)**·<290 1333. 캡 근원=**프롬프트 지시**(`prompts.py:23,66` "max 300 chars"), DDL/코드 절단 아님 | ⚠️ v1 길이=300 **우측검열**(23% 캡). 자연 길이 산출 불가. **다운스트림 이미 `[:100]`/`[:200]` 절단**(tasks.py 389/472) → 길이 확장=표시 무영향, grounding·저장만 |
| **[변동]** ⑶ | 덮어쓰기 vs 병존 | 저장=`validator_track_a.py:203` **`bulk_create`**·`update_or_create` 없음·**tuple unique 없음**. 동일(문서·source·target·관계) 중복 그룹 **104·232행**(최다 4/관계=자연 다중인용). `rematch`만 `.delete()`·이력 테이블 없음 | v2 재추출은 v1 삭제 안 하면 **병존(중복 누적)** → 대체/병존 정책 결정 필요 |
| **[비용]** ⑷ | 전량 재추출 비용 | 재카운트: deterministic_v1 **1751행 / distinct filings 351**(전체 356·평균 **4.97 cites/filing**). v1 추출 토큰 로그 **미보존** → G-e 표본 basis $0.0094/filing × 351 ≈ **$3.3**(351콜) | 1751 참고치 정확(351 filings 일치). **과대추정**(표본=cite-rich 24 vs 4.97) → 실제 ≤$3.3 |

## 채택 결정 (병진 승인)

- **D-SECB-V2-LEN = C**: 300자 캡 제거. sanity 경고선 **2,000자**(로그만·절단·거부 금지). 근거: 캡은 프롬프트 아티팩트(23% 검열)·다운스트림 이미 절단이라 길이 확장 무해·verbatim 매칭 개선 방향.
- **D-SECB-V2-COEXIST = B**: v1 **보존**·소비측 v2 필터. 근거: 모델에 이력 없음·해자 시점보존 원칙·v1 삭제=비가역. 소비 지점 v2 필터로 이중집계 차단.
  - **구현 = supersession (D-SECB-V2-CURRENT, (가)→(나) 전환)**: `.current()`=filing에 v2 있으면 v2·없으면 v1. naive `filter(v2)`는 v1-only 과잉배제(테스트 13건·창 회귀 실증)로 폐기. cf. DECISIONS D-SECB-V2-CURRENT.
- **비용 go**: ≤$3.3·351콜·**100건 체크포인트**(1단 자동정지).
- **V-B**: STANDBY 유지(트리거=롤아웃 후 nf율 >15%).
