# SLICE 19C 지시서 — 배치 엔진 v2: 드로다운 비례 다이얼 + 손잡이 5종 + 원장 2종

> **세션 종류:** 실행 세션. 설계 결정은 2026-07-14 디렉터·사용자 사이클에서 전부 닫혔다(§4 — 사용자 발안 다수). 이 세션은 결정을 다시 열지 않는다. 단 STEP 0가 전제를 깨면 HALT(§7).
> **1차 소스:** 결정 = `DECISIONS.md § SLICE19C`, 요약 = `DESIGN_SUMMARY_19C.md`, 계약 = `OUTPUT_CONTRACT_V3.md`(Part F).
> **base:** `ef8990c`(2026-07-16 재측정), pytest **602 green**. **LLM 비용:** 결정론 엔진만 — LLM 0.

## 왜 이 슬라이스인가
19a·19b 엔진은 임계값(현금 10%·집중도 30%)이 하드코딩된 고정 가치관. 사용자 확정 방향 —
- **다이얼:** "내 KRW 자산이 줄어든 만큼 비례해서 더 공격적으로"(자산 3% 감소 → 여력 3%p 확대). 환율 하락도 KRW 자산 감소로 자동 반영(emergent).
- **성향의 사용자 주권:** 공격성·상승장 태도·분산·집중 허용·탐험 배정 = 전부 사용자 손잡이(기본 보수). 엔진 고정 가치관 단 둘: "현금 3%는 남긴다" + "사실은 숨기지 않는다".
- **원칙 3계층 재정의:** ⑴ 환율 방향 예측 = 영구 금지. ⑵ 환율 수준 반응 = 허용. ⑶ 종목 수익성 예측 = 목표(정본 신호 위·유령/프록시 금지 + 기록). ⑶ 구현 = `SIGNAL-FORWARD-INFRA`(19c 밖).
- **탐험 레인:** 신뢰도 지배 랭킹이 젊은 후보를 배제하는 충돌 → 신뢰도 무보정, 여력의 E 비율을 젊은 후보 전용 레인으로 분리.

## 0. DoD (요약 — 상세는 DESIGN_SUMMARY / DECISIONS)
1. 원장 2종: `PortfolioSnapshot`(user·date·총자산 KRW·통화별·종목별 JSON, unique(user,date), nightly+엔진 upsert)·`AdvisoryRun`(실행시각·snapshot·산출·손잡이 스냅·레인).
2. UserGoal 손잡이 5종(A/G/w/L/E 가산·검증기·기본 보수).
3. 다이얼 산식(dd flow 분해·조정 고점·버퍼 max(10%−a,3%)·여력 클램프·가격 신선도 동결).
4. 게이트 = L 기반(자격+TRIM, 100=무제한). dd에 의한 L 자동 완화 없음.
5. 코어 랭킹 하이브리드 B′: `(1−w)×(신뢰도 0.60+진입가 0.25+통화여력 0.15) + w×분산 한계효과`. 신뢰도 지배 불변식(w≤0.20).
6. 탐험 레인 E(젊음 = STEP 0 (c) 기준, 레인 내 서열 = 진입가·통화여력).
7. 사실 보고(최대 집중도 항상·자동장치·손잡이 근거 문구).
8. 산출 계약 v3.
9. (부가) TRIM 기여도 분해.
10~11. 테스트 전수 + 불가침 불변식 + 회귀(602 → 종료, 의도 갱신 목록 선언).
12. 이월: SIGNAL-FORWARD-INFRA(우선순위 상승)·FX-매크로(b)·19d·세금/수수료.

## 불가침 불변식
버퍼 바닥 3% / 신뢰도 지배(w 상한 0.20) / 사실 보고. 손잡이 자동조정 금지(자동 반응은 dd만). 환율 방향 예측·유령/프록시·신뢰도 보정 금지. apps→shared 단방향.

## 실행 계획
- **A**: 결정 선행 기록(DECISIONS·TASKQUEUE·문서) + 회귀.
- **B**: 원장 2종 + 손잡이 5종 + 검증기. makemigrations(신규 2 + UserGoal 가산만) --check clean.
- **C**: 스냅샷 파이프라인(엔진 upsert + nightly) + flow 분해 + dd + 고점 리셋 + 신선도 동결.
- **D**: 다이얼(a·버퍼·클램프) + A·G 소비 + 통화별 배분.
- **E**: L 게이트화 + 코어 랭킹(하이브리드·w 재정규화·분산 한계효과) + 탐험 레인.
- **F**: 계약 v3 + AdvisoryRun 기록 + 테스트 전수 + 불가침 불변식.
- **G**: 닫기 보고 + health_check + 아키텍처.

## HALT 트리거
STEP 0 (c) 젊음 재료 부재 / (f) 유사 모델 중복 / baseline red / 손잡이 자동조정 필요 / 불가침 위반 / 의도 갱신 목록 밖 기존 테스트 깨짐 / 환율 방향 예측·프록시 필요 / shared 역참조·prod 필요(후보 보고).

## 다음 (범위 밖)
랜딩 mgmt 세션 → 결정 사이클(SIGNAL-FORWARD-INFRA vs Slice 20 순서) → 19d(사후분석 재보정).
