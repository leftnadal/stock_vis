# docs/instructions — 완료 슬라이스 지시서 정착소 (D-DOCS-PERSIST)

> **정책(D-DOCS-PERSIST, 2026-07-14):** 완료된 슬라이스의 실행 지시서·설계 목업은 여기(`docs/instructions/`)와
> `docs/design/`에 정착시킨다. fresh 세션이 지시서·목업을 못 봐 반복 재질의하던 문제를 종결한다.
>
> ⚠️ **진실의 소스는 여전히 `DECISIONS.md`.** 여기의 지시서 md는 **이력 참고물**(그 슬라이스가 무엇을·왜
> 하려 했는지의 스냅샷)이며, 결정의 정본이 아니다. 지시서와 DECISIONS가 어긋나면 DECISIONS가 이긴다.
> (기존 "지시서는 휘발(repo 밖)" 관례를 대체 — 근거: 세션 가시성.)

## MP2-ANALOG (유사 국면 카드) 슬라이스 지시서

| 슬라이스 | 파일 | 상태 |
|---------|------|------|
| C-core (L2 국면 카테고리 + FE 태그) | [`analog_slice_c_core.md`](analog_slice_c_core.md) | ✅ 정착됨(본 세션) |
| A (SPY 복원) | — | 세션 전달물(파일 미전달) → 디렉터 재전달 대기 |
| B (카드 결정론 코어) | — | 세션 전달물(파일 미전달) → 디렉터 재전달 대기 |
| C (라벨 파이프라인 원안, 3분할 전) | — | 세션 전달물(파일 미전달) → 디렉터 재전달 대기 |
| C-N (뉴스 소급 백필) | `docs/market_pulse_v2/slice_c_step0_findings.md`(C-N 브랜치) | 부분(STEP 0 findings만, 지시서 본문 미전달) |

> 미전달 항목: 해당 지시서는 채팅으로 전달되어 repo 파일이 아니었다. 디렉터가 파일로 재전달하면 여기에 정착한다.
